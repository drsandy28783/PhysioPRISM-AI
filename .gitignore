# — Python bytecode/cache —
__pycache__/
*.py[cod]
*$py.class

# — C extensions —
*.so

# — Distribution / packaging —
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# — Installer logs —
pip-log.txt
pip-delete-this-directory.txt

# — Test / coverage reports —
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# — Translations —
*.mo
*.pot

# — Django stuff (if ever used) —
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# — Flask stuff —
instance/
.webassets-cache

# — Local DBs & PHI risk —
*.db
physio.db
physio_backup.db

# — Service‑account credentials (Firebase, GCP, etc.) —
serviceAccount.json
credentials.json
firebase_config*.json

# — Environment files —
.env

# — Replit metadata (not needed in Git) —
.replit
uv.lock

# — Editor / OS files —
.vscode/
.idea/
*.iml
.DS_Store

# — SSL / key files —
*.pem
*.key

# — Misc logs —
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
